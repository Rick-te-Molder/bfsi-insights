npx lint-staged

# Check for large files in staged changes (BLOCKS commit if new code violates SIG guidelines)
node scripts/check-large-files.cjs

# Check if schema.md needs updating after migration changes
node scripts/check-schema-sync.cjs
